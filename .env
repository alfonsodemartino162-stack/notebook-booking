# Cambia queste per la produzione
ADMIN_EMAIL=admin@demo.local
ADMIN_PASSWORD=changeme
JWT_SECRET=cambia_questa_chiave_super_segreta

# 1 per ricreare il DB ad ogni avvio (svuota i dati!)
RESET_DB=0

# Fuso orario coerente per la conversione a UTC
TZ=Europe/Rome

# Render/Heroku usano PORT; in locale va a 3000 se non presente
# PORT=3000

